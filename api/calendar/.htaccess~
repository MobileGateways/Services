
# Turn on URL rewriting
RewriteEngine On

# service directory
RewriteBase /api/v1.0/calendar/

# Rewrite Service URIs
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule .* service.php/$0 [L]
